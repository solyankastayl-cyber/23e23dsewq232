"""
ta_module — package marker for TA module-level utilities.

This package will progressively house everything that belongs strictly to the
TA / Trading vertical (analysis · prediction · decision · execution ·
analytics · admin) as we walk Phase A → E from plan_project.md.

Phase A.1 — Backend Namespace (alias layer)
   See `ta_namespace.py`.
"""
